# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Grok (xAI) — live event discovery (Responses API + web_search)
XAI_API_KEY=
# Optional override, default grok-3
# XAI_MODEL=grok-3

# Optional web search + API fillers
TAVILY_API_KEY=
TICKETMASTER_API_KEY=
EVENTBRITE_API_KEY=

# Protect /api/cron/ingest
CRON_SECRET=change-me-to-a-long-random-string

# Legacy local Prisma (optional fallback)
# DATABASE_URL="file:./dev.db"
